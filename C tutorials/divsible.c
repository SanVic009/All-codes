# include<stdio.h>

int main(){
    int A;
    printf("Please enter the number to check its divisibility with 2:");
    scanf("%d", &A);

    printf("%d", A%2==0);
    return 0;
}
// to future sanchit, make this code better. Rather than just printing the remainder, make it print a statement