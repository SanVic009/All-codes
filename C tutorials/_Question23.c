#include <stdio.h>
#include <string.h>

// 8:15:30 on youtube time

int main()
{
    int *password[100];
    printf("Please enter your password to be salted :")
        fgets(password, 100, stdin);
    int i = 0;
    while (ch != '\n')
    {
        scanf("%c", &ch);
        s[i] = password;
        i++;
    }
    return 0;
}